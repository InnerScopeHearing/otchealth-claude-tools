# Node deps (installed by setup/session-start.sh at session start)
node_modules/
package-lock.json

# NEVER commit real credentials
credentials.env
*.credentials.env
.designer/
.gcp_*.json
*-sa.json
gcp-*-sa.json
*.pem
*.p12

# Generated assets (designer output lands in project repos, not here)
assets/generated/
*.log
.DS_Store

# COO confidential runtime notes — sensitive specifics never go in git
*.local.md
coo/CONFIDENTIAL*

# locally-generated synthetic fixture bundles (regenerate with seed-fixtures.mjs)
skills/synthetic-health-data/fixtures/
