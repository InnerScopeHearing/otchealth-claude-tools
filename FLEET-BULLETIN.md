# Fleet Bulletin

CTO -> fleet changelog. One line per fleet-affecting change (a new SOP, a tooling change, a
decision every agent must honor). Travels with claude-tools; every agent's octools-sync surfaces
new entries on its next prompt, so the whole fleet stays on the same page without a restart.
Write with: node setup/bulletin.mjs add "<line>"

- 2026-06-22T15:00Z | LIVE-SYNC is on: the shared toolkit now auto-refreshes mid-session (octools-sync, a UserPromptSubmit hook) instead of only at session start. When the CTO merges a change to claude-tools/main, every running agent picks it up on its next prompt, no restart. This bulletin is how fleet-affecting changes are announced.
- 2026-06-22T15:24Z | Fleet rollout: every app session now auto-installs the octools-sync live-sync hook (via session-start -> ~/.claude/settings.json), so ALL app agents live-pull claude-tools/main per prompt. No per-app setup needed.
- 2026-06-22T15:48Z | Working memory now WARNS LOUDLY when KB_AGENT is unset (was silent -> caused the CFO memory loss). If your session prints 'WORKING MEMORY IS OFF', set KB_AGENT to your role (cfo|clo|cto|coo) in your environment config and restart, or set KB_MEMORY_OPTOUT=1 if you intentionally run without memory.
- 2026-06-22T15:48Z | Autonomous/overnight runners (autonomous-run.yml, overnight-agent.yml) now have a post-run SAFETY NET: any work the headless agent leaves is auto-captured onto a claude/* branch as a DRAFT PR, so unattended runs never silently lose their output at teardown. Never pushes to the default branch.
- 2026-06-22T19:17Z | Working memory now resolves the agent PER SESSION (most-specific wins): ~/.claude/.kb-agent marker > repo .kb-agent > shared KB_AGENT env. Because all agents share ONE cloud environment, a single KB_AGENT cannot label everyone -> each session claims its identity: 'mkdir -p ~/.claude && echo <role> > ~/.claude/.kb-agent' (cto|cfo|clo|coo). App repos can commit a .kb-agent. A marker/repo default overrides the shared env var; a mismatch is surfaced, not silently honored.
- 2026-06-23T00:11Z | kb-memory HARDENED (memory was silently failing): mem.mjs now self-resolves the claude-driver SA from ~/.gcp_claude_driver_sa.json when the env var is unset (no more silent 'memory off' on a fresh shell). New verbs: 'whoami --agent <role>' (one-line health check: identity+SA+ledger count+PASS/FAIL) and 'use <role>' (claim this session's identity). NEW: run 'bash /tmp/octools/setup/agent-activate.sh <role>' at the start of EVERY session - it force-syncs the toolkit to main, claims identity, and self-tests, so you never run on a stale branch or miss a file that exists on main.
- 2026-06-23T00:45Z | NEW master DEVELOPER agent: ONE app/web developer across the whole portfolio (a hive mind, one 'developer' memory lane) replaces the per-app App-Lead identities. Activate: bash /tmp/octools/setup/agent-activate.sh developer. App-specific context stays in each app repo; cross-app engineering lessons compound in the developer brain (write-through --agent developer --tags <app>). iOS builds + TestFlight stay CTO-only. Definition: dream-team/agents/developer.md.
- 2026-06-23T00:52Z | live-walkthrough is now a fleet skill (skills/live-walkthrough): Playwright WebKit multi-device interaction-QA; each app supplies its own qa/live-walkthrough/stubs.mjs + journeys.mjs, the probes/devices/runner/report are shared.
- 2026-06-23T01:22Z | Credential registry is now OFF NOTION: skills/vault-sync/vault-registry.mjs regenerates it from Secret Manager into the Azure brain (otchealthcommons/company-journal/_VAULT/registry.md, 159 creds across 37 services, brain-searchable via company-brain), wired into the nightly job. To FIND credentials: ask the company-brain or read _VAULT/registry.md, NOT Notion. The Notion registry DB is the legacy mirror (retiring August).
- 2026-06-23T02:20Z | ACTIVATION is the TRANSPARENT 3-step path, NOT the one-line wrapper. The auto-mode security classifier BLOCKS 'bash /tmp/octools/setup/agent-activate.sh' (opaque /tmp script that pulls main then executes fetched code = correct supply-chain guard). Activate instead: (1) git -C /tmp/octools fetch origin main && git -C /tmp/octools reset --hard origin/main  (2) node /tmp/octools/skills/kb-memory/mem.mjs use <role>  (3) node /tmp/octools/skills/kb-memory/mem.mjs whoami --agent <role> -> RESULT: PASS. Then recall/team/brain. Wrapper still works where a Bash allow-rule for /tmp/octools exists; the 3 steps work everywhere. SKILL.md + developer.md + the script header now lead with the 3 steps.
- 2026-06-23T03:23Z | COO routine is OFF NOTION: the 'COO Tasks' + 'Bucket Briefings' Notion DBs are retired. The COO's durable state, tasks, dispatches, and briefings now live in the kb-memory 'coo' ledger (auto-shared to the exec feed + brain) + the coo/ files. Briefings = 'mem.mjs status --agent coo'; dispatch = 'decision --share --tags dispatch,<to>'; approvals '--tags needs-matt'; failure alert = 'decision --share --tags needs-matt,alert'. The morning-brief idempotency marker is the file coo/morning-marker.md (write today's date to claim, clear on failure). NOTE: the LIVE Claude Code Routine prompt + any n8n node writing to Notion still need a one-time re-paste/rewire (CTO+Matt).
- 2026-06-23T04:08Z | session-start.sh now FAILS LOUD on a malformed/blank claude-driver SA instead of silently leaving memory off. It validates GCP_CLAUDE_DRIVER_SA_JSON is parseable JSON before writing the SA file; if it is set-but-not-JSON (the common .env mistakes: wrapping quotes, backslash-escaped quotes, or a multi-line paste) or unset, it prints a big actionable ERROR/WARN with the exact fix (raw JSON, ONE line, NO quotes, name spelled GCP_CLAUDE_DRIVER_SA_JSON with a leading G) and detects a near-miss var name like CP_CLAUDE_DRIVER_SA_JSON. ENV SETUP REMINDER: the SA goes in the environment's Environment variables (.env format, no quotes, one line); changes apply to NEW sessions only.
- 2026-06-23T05:20Z | agent-activate.sh is now PATH-ROBUST: it prefers the disposable /tmp/octools clone (force-synced to main) but FALLS BACK to an attached claude-tools checkout (/home/user/otchealth-claude-tools) WITHOUT resetting it, so activation works in sessions where /tmp/octools is not populated (e.g. the Developer, which attaches the claude-tools repo). Use that checkout path in the transparent activation commands when /tmp/octools is thin. Plus a Notion sweep: the CLO kickoff + plugins audit no longer point agents at the retired Notion matter-vault / COO Tasks / Bucket Briefings (legal -> Azure legal store via the legal skill; credentials -> Secret Manager + brain _VAULT/registry.md; briefings -> the kb-memory exec feed).
- 2026-06-24T05:40Z | DOCUMENT PIPELINE now works for every agent: session-start.sh auto-installs the document essentials the container base image was MISSING - LibreOffice writer/calc/impress (Office docs; the base had only -core/-common, so soffice --convert-to failed to load ANY file), poppler-utils/pdftotext (PDF text extraction), and weasyprint (HTML/Markdown -> PDF rendering, the pdf skill's CREATE path). These broke silently for every agent (CLO hit the soffice half on a legal template; Developer hit the PDF-render half). Guarded (skips when present, warm containers pay nothing) + non-fatal; a fresh container self-heals at session start. Heavy/rare deps (tesseract OCR - cloud covers it; ffmpeg - video) stay lazy-installed by their skills: premium essentials always-on, not the kitchen sink.
- 2026-06-24T06:43Z | DOC INTELLIGENCE deep-pass shipped (skills/doc-indexer/deep-pass.mjs + job/deep-pass.sh): the fix for botched gpt-4.1-mini librarian summaries. Re-summarizes legal+finance docs on gpt-4.1 (rich, faithful, decision-grade, quotes exact figures), runs a gpt-4.1 VISION signature/execution pass (requires/has/WET/DIGITAL + signatories + execution date), proposes canonical filenames (executionDate_Title.ext), emits dedup keys, and CONFIDENCE-GATES low-confidence/outlier docs to NEEDS_CLAUDE_REVIEW -> _REVIEW/review-queue.csv (the CLO/CFO 'job one' list) instead of guessing. Resumable, Azure credits ~$350 for all ~22k docs, $0 cash, zero Max draw. Old mini summary kept as summary_mini. INTERIM until a room's deep pass completes: treat its summaries as suspect, open the source doc.
- 2026-06-24T19:43Z | DOC DEEP-PASS COMPLETE + reindexed for CFO finance + CLO legal (company+personal): every doc re-summarized on gpt-4.1 (rich, figure-quoting, decision-grade), signature/execution taxonomy (wet/digital/unsigned), re-OCR healed scans, non-text assets (audio/photos/archives) classified OUT of the review queue, content-filter-blocked docs flagged for manual review. The 'treat interim summaries as suspect' note is LIFTED - all 3 AI Search rooms (finance-cfo-source-docs, legal-company, legal-personal) serve the NEW summaries (verified via company-brain). Review queues are now the focused high-value lists (finance ~14%, legal-company 26%, legal-personal 18%). summary_mini kept for audit.
- 2026-06-24T19:43Z | ACTIVATION HARDENED (Matt directive 2026-06-24): never hand a copy-paste activation/sync step that can fail silently. setup/octools-bootstrap.sh is the ONE robust entrypoint - prefers the attached /home/user/otchealth-claude-tools checkout, clones LOUD with retries only if needed, self-tests, prints ===OCTOOLS OK=== / ===OCTOOLS FAIL=== with the exact reason. Env SessionStart hooks should call it instead of 'git clone ... 2>/dev/null'. If an agent says 'toolkit not synced', run the bootstrap (it tells you why); do not re-paste blind.
- 2026-06-24T19:43Z | doc-indexer reindex is now proxy-blip resilient: push/embed retry the agent-proxy's transient SSRF 403 (resolve_no_records / private-reserved-IP / EAI_AGAIN), which had killed a finance reindex at 92%. New push-search --skip N re-pushes only the tail after an interrupted reindex (idempotent mergeOrUpload) instead of redoing 16k. Embedding deployment text-embedding-3-large bumped 50K->1000K TPM (it was the reindex bottleneck); serialize big reindexes or give them TPM headroom.
- 2026-06-24T20:25Z | MEMORY IS NOW AUTO-CAPTURED (agents stop forgetting). The PreCompact + Stop hooks used to only NAG; they now CAPTURE every input+output to a durable timestamped per-day journal (kb-journal.mjs) AND distill durable facts/decisions to your ledger via reflect --commit (PreCompact always; Stop throttled ~15min). Installed USER-scope by install-octools-hook.mjs so the hooks fire in EVERY repo, not just claude-tools (that was the silent gap). A nightly memory-librarian Container Apps Job (cron 0 8) writes a per-agent DAILY DIGEST (_JOURNAL/<agent>/<date>/_DIGEST.md), backfills the ledger, reindexes the brain, and reports gaps. STILL REQUIRED per session: claim identity with 'mem.mjs use <role>' (or ~/.claude/.kb-agent / repo .kb-agent) or capture is OFF and the hook warns. clo-personal stays segregated (never shared/brain-folded).
- 2026-06-24T23:05Z | octools-bootstrap.sh now SELF-HEALS a stale attached checkout: if /home/user/otchealth-claude-tools is behind main with no local work it auto-switches you to a fresh /tmp/octools clone (ends the silent 'missing kb-journal/company-brain = it forgot' amnesia). Pure-stale = behind>0/ahead=0/clean; dev checkouts (CTO) are kept+warned, never reset. Use the curl-from-main bootstrap entry.
- 2026-06-24T23:49Z | Superbrain P0 SHIPPED: per-prompt memory injection. A new kb-recall UserPromptSubmit hook + 'mem.mjs pack' re-inject a ranked, RING-FILTERED, token-budgeted slice of your ledger into context on EVERY prompt (and the first prompt after a compaction), so agents stop 'forgetting 20 min ago'. LLM-free + local-cache-first (no Azure on the hot path). Beacon 'MEMORY: LIVE agent=.. | ledger=N' shows it is on; KB_MEMORY_OPTOUT=1 silences. Running sessions pick it up on next octools-sync (now re-runs the hook installer). Still needs your identity marker: echo <role> > ~/.claude/.kb-agent.
- 2026-06-25T02:59Z | Two repo helpers shipped. (1) setup/repo-freshen.sh runs at SessionStart and SAFELY fast-forwards your OWN app repo to origin/main when it is a pristine stale session branch (fixes 'my branch is 50 commits behind main'); it NEVER touches a branch with local commits or a dirty tree (it prints the exact catch-up command). OCTOOLS_NO_REPO_FRESHEN=1 disables. (2) setup/add-repo.sh <repo> [branch] clones any InnerScopeHearing org repo into the sandbox on demand via the org GitHub-App token - the always-works 'add a repo' path when the add_repo session tool is not exposed (working tree + push; MCP repo-scope is still add_repo/the env repo list).
- 2026-06-25T03:33Z | Memory self-heal: a missing identity marker no longer means silent-off. kb-inject/kb-recall now AUTO-CLAIM your agent from the repo name for UNAMBIGUOUS single-agent repos (otchealth-cto->cto, the app repos->developer, otchealth-ops->coo, otchealth-legal->clo); ambiguous or PHI repos (medreview, otchealth-exec) stay loud-OFF and are NEVER mis-homed. The resolver is centralized in skills/kb-memory/agent-id.sh (add a repo mapping there). Explicit ~/.claude/.kb-agent and KB_AGENT still win; KB_NO_AUTOCLAIM=1 disables.
- 2026-06-25T03:37Z | Operator memory-health view: 'node mem.mjs team-health' shows a LIVE/STALE line per exec agent (last shared activity + what they are working on), --json for the brief. COO: add this to the morning brief so Matt sees a green/red line per agent at a glance. Also tests/memory-loop.test.mjs now locks the remember->compaction->recall read-back loop in CI, so the anti-forgetting fix can never silently regress.
- 2026-06-25T03:49Z | Memory health now streams to PostHog in REAL TIME: a new memory_beacon event (agent, status LIVE/DARK, ledger_size, last_write_age, hooks_wired) emits to the Fleet Agents project (479484) from the Stop hook, self-throttled ~10min + backgrounded (never on the prompt hot path). This is the operator-dashboard signal AND the foundation for the coming auto-medic (a standing process that watches this stream and auto-dispatches the medic when an agent goes DARK / off-the-rails).
